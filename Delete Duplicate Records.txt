-- DELETE Duplicate records
SELECT * from movie where (seatno, rowno) in (
SELECT seatno, rowno FROM(
  SELECT SEATNO,ROWNO,ROW_NUMBER()OVER(PARTITION BY ROWNO ORDER BY ROWNO,SEATNO DESC) R,
  ROW_NUMBER()OVER(ORDER BY ROWNO,SEATNO DESC) ID FROM MOVIE
)M1
WHERE M1.R >1
)
